[project]
name = "djangotutorial"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.4",
]

[dependency-groups]
dev = [
    "django-stubs>=6.0.4",
    "ruff>=0.15.12",
]
